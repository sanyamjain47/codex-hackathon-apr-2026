{
  "name": "codex-hackathon-apr-2026",
  "version": "0.1.0",
  "private": true,
  "description": "Workspace for Codex Git diff viewer plugin experiments.",
  "scripts": {
    "dev": "npm run dev --workspace plugins/git-diff-viewer",
    "dev:viewer": "npm run dev --workspace @git-diff-viewer/app",
    "dev:app": "npm run launch --workspace git-diff-viewer",
    "build": "npm run build --workspace plugins/git-diff-viewer",
    "typecheck": "npm run typecheck --workspace plugins/git-diff-viewer"
  },
  "workspaces": [
    "plugins/git-diff-viewer",
    "plugins/git-diff-viewer/viewer"
  ]
}
